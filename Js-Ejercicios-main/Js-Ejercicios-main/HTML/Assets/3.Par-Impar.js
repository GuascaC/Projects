'use strict';
let num=0;
num=prompt("Introduzca un número");

    function par(num) 
    { 
      if (num % 2 == 0) return alert("Par:"+num); 
      else{ return alert("Impar:"+num)}
    } 
let result = 0;
result = par(num);